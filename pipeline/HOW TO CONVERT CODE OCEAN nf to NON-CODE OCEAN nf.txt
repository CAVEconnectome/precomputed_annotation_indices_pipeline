Point the Docker container command (the image pull) at the DockerHub image instead of the Code Ocean image:
Find (the GUID might change over time):
	container "$REGISTRY_HOST/capsule/1c40bced-b7eb-41cb-9d2d-1d714f8e31e8:94b89e4eedd63b0fd3d7ea314137edb6"
Replace:
	container "kbwiley/precomputed-annotation-indices:1.0.0"




Point the Github clone command (the repo pull) at the non-Code Ocean Capsule repo instead of the Code Ocean Capsule repo:
Find:
	if [[ "\$(printf '%s\n' "2.20.0" "\$(git version | awk '{print \$3}')" | sort -V | head -n1)" = "2.20.0" ]]; then
		git -c credential.helper= clone --filter=tree:0 "https://\$GIT_ACCESS_TOKEN@\$GIT_HOST/capsule-5443678.git" capsule-repo
	else
		git -c credential.helper= clone "https://\$GIT_ACCESS_TOKEN@\$GIT_HOST/capsule-5443678.git" capsule-repo
	fi
	git -C capsule-repo checkout f4d1eaedaeebd78f7878eda50a0649cb0bc5e412 --quiet
	mv capsule-repo/code capsule/code && ln -s \$PWD/capsule/code /code
Replace:
	git -c credential.helper= clone https://github.com/CAVEconnectome/precomputed_annotation_indices_capsules.git capsule-repo
	mv capsule-repo/code capsule/code && ln -s $PWD/capsule/code /code

NOTE: It's possible one process/capsule will lack the "git -C capsule-repo checkout" and fail the search above. I'm not sure why this single process/capsule differs from the others, but keep an eye out for it.



Add the path1 variable to the first capsule:
Add the following between the memory and output commands of the first capsule:
	input:
	val path1


Add the data config link to the first capsule:
Add the following below the multiple 'mkdir' commands in the first process:capsule:
	ln -s "/tmp/data/${params.data_name}__data-config/$path1" "capsule/data/$path1" # id: 7c301f12-d69e-4e51-9b9d-9df03066bc1b

Alter the data link on the fourth capsule:
Search for "ln -s "/tmp/data/", below the multiple 'mkdir' commands in the fourth process/capsule:
	ln -s "/tmp/data/${params.datasubset_name}" "capsule/data/${params.datasubset_name}" # id: bacb8982-892d-437b-b732-de1257e7a398

Alter the results paths:
Find:
	publishDir "$RESULTS_PATH", mode: 'copy', saveAs: { filename -> new File(filename).getName() }
Replace:
	publishDir "${params.results_path}", mode: 'copy', saveAs: { filename -> new File(filename).getName() }